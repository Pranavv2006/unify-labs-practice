# Copy this file to .env and fill in your values
# cp .env.example .env

# MongoDB Atlas connection string
# Get it from: https://cloud.mongodb.com → Clusters → Connect → Drivers
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority

# Port for local development (Vercel ignores this)
PORT=3000

# Secret token that protects the POST /api/seed endpoint.
# Generate a strong random string, e.g.: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Add this same value in Vercel Dashboard → Settings → Environment Variables.
SEED_SECRET=change_me_to_a_long_random_string
